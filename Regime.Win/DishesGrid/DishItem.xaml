<UserControl x:Class="Regime.Win.DishesGrid.DishItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:Regime.Win.Models"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="800">
    <Border BorderThickness="1" BorderBrush="Black"
            d:DataContext="{d:DesignInstance {x:Type models:DishViewModel}}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" TextWrapping="Wrap" FontWeight="Bold" Text="{Binding Dish.Caption}"/>
            <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="15 2 2 2" Text="{Binding IngredientsString}"/>
            <TextBlock Grid.Row="2" TextWrapping="Wrap" Margin="15 2 2 2" Text="{Binding TotalSummary}"/>
        </Grid>
    </Border>
</UserControl>
